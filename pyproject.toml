[project]
name = "django-devops-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "django-redis>=6.0.0",
    "python-decouple>=3.8",
    "requests>=2.32.5",
]
